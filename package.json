{
  "name": "repopilot",
  "version": "0.1.0",
  "private": true,
  "description": "Repository-level AI coding agent",
  "packageManager": "pnpm@10.10.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}